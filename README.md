# techdegree-unit-4-web-style-guide

The web style guide is a project in which I have transferred a website built with CSS and HTML to SASS. I have transferred all the CSS files to SCSS partial files which makes the website easier to maintain, manage and adjust to specific needs. 
